<?xml version="1.0" encoding="utf-8"?>
<!-- One charge limit (AC or DC), opened by tapping its figure on the home
     screen. A stepper over the six values the car accepts, then Set sends it. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/card"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="20dp"
    android:paddingEnd="20dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/limit_title"
        style="@style/CardLabel"
        tools:text="AC charge limit" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/limit_value"
            style="@style/CardFigure"
            android:layout_width="0dp"
            android:layout_weight="1"
            tools:text="80%" />

        <ImageButton
            android:id="@+id/limit_down"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/icon_button"
            android:contentDescription="@string/charge_limit_lower"
            android:scaleType="center"
            android:src="@drawable/ic_minus"
            android:tint="@color/text" />

        <ImageButton
            android:id="@+id/limit_up"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/icon_button"
            android:contentDescription="@string/charge_limit_higher"
            android:scaleType="center"
            android:src="@drawable/ic_plus"
            android:tint="@color/text" />

    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/charge_limit_hint"
        android:textColor="@color/text_muted"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:gravity="end">

        <TextView
            android:id="@+id/limit_cancel"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackground"
            android:fontFamily="@font/manrope_semibold"
            android:gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="@string/charge_limit_cancel"
            android:textColor="@color/text_dim"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/limit_set"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:background="?android:attr/selectableItemBackground"
            android:fontFamily="@font/manrope_semibold"
            android:gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="@string/charge_limit_set"
            android:textColor="@color/accent"
            android:textSize="15sp" />

    </LinearLayout>

</LinearLayout>
