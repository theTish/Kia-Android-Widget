# THIS is the file Vercel installs from. vercel.json points @vercel/python at
# api/index.py, and the builder reads the requirements.txt next to the
# entrypoint - the one in the repo root is for Render/Procfile (main.py) and is
# ignored here. Keep the two in step.
Flask>=3.0.0
hyundai-kia-connect-api==4.31.0
requests>=2.31.0
tzdata>=2023.3
gunicorn>=21.2.0
dnspython>=2.4.0
