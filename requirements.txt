# Used by Render/Procfile, which run main.py. Vercel does NOT read this file -
# it installs from api/requirements.txt, next to the entrypoint in vercel.json.
# Keep the two in step.
Flask>=3.0.0
hyundai-kia-connect-api==4.31.0
requests>=2.31.0
tzdata>=2023.3
gunicorn>=21.2.0
dnspython>=2.4.0
